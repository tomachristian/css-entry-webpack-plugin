import ModuleReason from "./ModuleReason";

export default class Module /*extends DepedenciesBlock*/ {
    context: string | null;
    reasons: ModuleReason[];

    /* identifier = null */
    /* readableIdentifier = null */
    /* build = null */
    /* source = null */
    /* size = null */
    /* nameForCondition = null */
}
